<?xml version="1.0" encoding="utf-8" ?>
<app:ContentPageBase
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="MobileKidsIdApp.Views.BasicDetailsPage"

    xmlns:app="clr-namespace:MobileKidsIdApp"
    Title="Basic details">

    <ScrollView
        Margin="20,20,20,0">

        <StackLayout>
            <Entry
                Placeholder="Given"
                Text="{Binding Child.GivenName}" />
            <Entry
                Placeholder="Nickname"
                Text="{Binding Child.NickName}" />
            <Entry
                Placeholder="Middle"
                Text="{Binding Child.AdditionalName}" />
            <Entry
                Placeholder="Family"
                Text="{Binding Child.FamilyName}" />

            <!-- TODO: Take DatePicker fixes from BSCI projcet using material renderers -->
            <DatePicker
                Date="{Binding Child.Birthday}"/>

        </StackLayout>
    </ScrollView>

</app:ContentPageBase>